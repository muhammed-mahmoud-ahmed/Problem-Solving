#include<iostream>
using namespace std;
int main()
{
    long long x;
    cin>>x;
    cout<<x*(x+1)/2;
    // if i solved this with for loop it will exceed the available run time!
    
    // for(int i=1;i<=x;i++)
    // {
    //     //i tried to put summation in x but of i changed the value of x it will change in loop statement 
    //     sum=sum+i;

    // }
    // cout<<sum;
}